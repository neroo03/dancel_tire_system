{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch in Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost/dancel_tire_system/login.php",
            "webRoot": "${workspaceFolder}"
        }
    ]
}